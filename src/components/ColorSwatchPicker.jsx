// Color swatch picker for player selection (skeleton)
export default function ColorSwatchPicker({ availableColors, selectedColor, onSelect }) {
  return (
    <div className="flex flex-wrap gap-2">
      {/* TODO: Render swatches, highlight selected, call onSelect */}
    </div>
  );
}
