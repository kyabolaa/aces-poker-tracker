// Ledger for entering final chip counts (skeleton)
export default function ChipCountLedger({ players, onChange }) {
  return (
    <div className="w-full flex flex-col gap-2">
      {/* TODO: Render player rows with input, live win/loss calculation */}
    </div>
  );
}
