// Player row with name, color, buy-in count, plus/minus buttons (skeleton)
export default function PlayerRow({ player, onIncrement, onDecrement }) {
  return (
    <div className="flex items-center gap-4 bg-gray-900 rounded-xl p-3 mb-2">
      {/* TODO: Show color swatch, name, buy-in count, animated +/- buttons */}
    </div>
  );
}
