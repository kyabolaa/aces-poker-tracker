// Table displaying added players with edit/delete controls (skeleton)
export default function PlayerTable({ players, onEdit, onDelete }) {
  return (
    <div className="w-full bg-gray-900 rounded-xl p-4 mt-4">
      {/* TODO: Render player rows, edit/delete buttons */}
    </div>
  );
}
